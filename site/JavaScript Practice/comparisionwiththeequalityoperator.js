//there are many comparison operator in Javascript All of these operatores return a boolean true or false value

// the most basic operator is equality operator == the equality operator compare two values and return true if they are equavalent or false if they are not.

/* Note: equality is different from assignment (=) which assigns the value on the right of the operator to a variable on the left.*/

function equalitytest(myValue){
    
}